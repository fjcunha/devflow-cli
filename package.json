{
	"name": "@fjcunha/devflow-cli",
	"version": "1.0.0",
	"description": "Official npm wrapper for DevFlow - A Multi-Agent System for Claude Code",
	"type": "module",
	"license": "MIT",
	"author": "Fabrício Cunha",
	"keywords": [
		"cli",
		"devflow",
		"claude",
		"ai",
		"multi-agent",
		"claude-code",
		"developer-tools",
		"project-initialization"
	],
	"repository": {
		"type": "git",
		"url": "git+https://github.com/fjcunha/devflow-cli.git"
	},
	"bugs": {
		"url": "https://github.com/fjcunha/devflow-cli/issues"
	},
	"homepage": "https://github.com/fjcunha/devflow-cli#readme",
	"publishConfig": {
		"access": "public",
		"registry": "https://registry.npmjs.org/"
	},
	"bin": {
		"devflow": "./dist/cli.js"
	},
	"files": [
		"dist"
	],
	"scripts": {
		"build": "tsup src/cli.ts --format esm",
		"dev": "tsx src/cli.ts",
		"prepublishOnly": "npm run build",
		"check": "echo \"✅ Type checking (tsc)...\" && tsc --noEmit && echo \"🧹 Lint checking (biome)...\" && biome check && echo \"🧪 Running tests (vitest)...\" && vitest run && echo \"🔍 Running package audit (npm audit)...\" && npm audit --audit-level high && echo \"🎉 All checks passed!\"",
		"test": "vitest",
		"test:run": "vitest run",
		"fix": "biome check --fix"
	},
	"engines": {
		"node": ">=18"
	},
	"dependencies": {
		"commander": "^12.0.0"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.3.11",
		"@types/node": "^20.0.0",
		"@vitest/coverage-v8": "^1.6.1",
		"@vitest/ui": "^1.6.0",
		"tsup": "^8.0.0",
		"tsx": "^4.7.0",
		"typescript": "^5.0.0",
		"vitest": "^1.6.0"
	},
	"overrides": {
		"esbuild": "0.27.2"
	}
}
